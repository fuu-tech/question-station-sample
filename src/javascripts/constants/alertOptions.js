export const SuccessOption = Object.freeze({
  position: 'bottom',
  timeout: 2000,
  effect: 'scale'
});

export const ErrorOption = Object.freeze({
  position: 'bottom',
  timeout: 3000,
  effect: 'scale'
});
