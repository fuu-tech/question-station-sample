export const contentSortTypes = Object.freeze({
  memberNum: "member_counter",
  newer: "id",
});

export const userSortTypes = Object.freeze({
  contentNum: "created_content_counter",
  newer: "id",
});

export const postSortTypes = Object.freeze({
  newer: "id",
});

export const workbookSortTypes = Object.freeze({
  newer: "id",
  contentNum: "content_counter",
});
